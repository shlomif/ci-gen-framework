#!/usr/bin/env perl6

use Getopt::Long;

use CI::Gen;
my %params;
my $o = Getopt::Long.new(
    'param=s%'
);
my $ret = $o.get-options(@*ARGS);
CI::Gen::CI-Gen.new(basedir=>".", params=>$ret.{'param'}.hash).generate("foo");
